export default function verificarConexao(): void {
    console.log("Verificando conexão com o banco de dados...");
    console.log("Conexão verificada com sucesso!");
}

//só pode ter um default por arquivo, se tiver mais de um, o typescript vai dar erro